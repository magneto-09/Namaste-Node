// Since, JS doesn't support enums. so with const this is the best we can do. use TS for enums.
const dummy3EmailEnums = ["gmail.com", "outlook.com", "hotmail.com"];

const dummy3UpdateProfileEnums = ["firstName", "lastName", "password"];

module.exports = {
  dummy3EmailEnums,
  dummy3UpdateProfileEnums,
};
